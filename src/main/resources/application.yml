
spring:
  application:
    name: Projekt 2019

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:8183/gaienhofen?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Berlin&useLegacyDatetimeCode=false
    username: root
    password: ""

  h2:
    console:
      enabled: true
      path: /h2
      settings:
        web-allow-others: true

  jpa:
    open-in-view: true
    show-sql: true
    hibernate:
      ddl-auto: none

  profiles:
   include: email


